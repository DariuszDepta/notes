# GitHub actions

## Recent versions

Last update: 2026-05-22

### actions/checkout 

> v6.0.2

To retrieve the latest version, run: 

```shell
curl -sSL https://api.github.com/repos/actions/checkout/releases/latest | jq -r '.tag_name'
```

### actions/setup-node

> v6.4.0

To retrieve the latest version, run:

```shell
curl -sSL https://api.github.com/repos/actions/setup-node/releases/latest | jq -r '.tag_name'
```

### actions/configure-pages

> v6.0.0

To retrieve the latest version, run:

```shell
curl -sSL https://api.github.com/repos/actions/configure-pages/releases/latest | jq -r '.tag_name'
```

### actions/upload-pages-artifact

> v5.0.0

To retrieve the latest version, run:

```shell
curl -sSL https://api.github.com/repos/actions/upload-pages-artifact/releases/latest | jq -r '.tag_name'
```

### actions/deploy-pages

> v5.0.0

To retrieve the latest version, run:

```shell
curl -sSL https://api.github.com/repos/actions/deploy-pages/releases/latest | jq -r '.tag_name'
```
